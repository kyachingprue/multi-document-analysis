npx create-next-app@latest docuai
Setup clerk
npx shadcn@latest init

#Database
https://www.prisma.io/docs/getting-started/prisma-postgres/quickstart/prisma-orm
npm install typescript tsx @types/node --save-dev
npm install prisma @types/node --save-dev 
npm install @prisma/client @prisma/adapter-pg dotenv

prisma - The Prisma CLI for running commands like prisma init, 
prisma migrate, and prisma generate
@prisma/client - The Prisma Client library for querying your database
@prisma/adapter-pg - The node-postgres driver adapter that
connects Prisma Client to your database
dotenv - Loads environment variables from your .env file

prisma.config.ts
prisma/schema.prisma
npx prisma migrate dev --name init - create first migration to setup the database tables
npx prisma generate - generate prisma client
You need to pass an instance of Prisma ORM's
driver adapter to the PrismaClient constructor:


#Gemini Setup
npm install @google/generative-ai

# Install Vercel Blob and other dependencies
npm install @vercel/blob
npm install file-saver


.env

# Clerk
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up


# GEMINI
GEMINI_API_KEY=

# Database
DATABASE_URL=

# Vercel Blob
BLOB_READ_WRITE_TOKEN=

# App
NEXT_PUBLIC_APP_URL=


# Hostinger VPS deployment
sh root@your_server_ip  Update System Packages
sudo apt update && sudo apt upgrade -y

# Install Node.js & npm (for running Next.js):
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Verify installation
node --version  # Should show v20.x.x
npm --version

sudo apt install git -y // giti

 #Set Up a Firewall (for security)
sudo ufw allow OpenSSH
sudo ufw allow 3000 
sudo ufw enable

cd /var
git clone https://github.com/dmalvia/multi-tenant-ai-document-analysis-platform.git  www
cd www
sudo nano .env

npm install
npx prisma generate  # Generate Prisma client for your cloud database
npm run build        # Build your Next.js app


# Install PM2 globally
sudo npm install -g pm2

# Start your app
pm2 start npm --name "next-app" -- start

curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb

sudo dpkg -i cloudflared.deb

cloudflared tunnel --url http://76.13.145.245:3000
